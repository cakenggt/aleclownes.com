# Site settings
baseurl: "" # the subpath of your site, e.g. /blog/

# Build settings
markdown: kramdown
highlighter: rouge

collections:
  stories:
    output: true

feed:
  collections:
    stories:
      path: "/stories/feed.atom"

plugins:
  - jekyll-feed
